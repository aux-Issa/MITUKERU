<header>
  <h1><%= "#{@city.prefecture.name}#{@city.name}の不動産会社" if @city.present? %> </h1>
</header>

<% if @shops.present? %>
  <%= render @shops %>
<% else %>
  <p>このエリアには表示できる不動産会社がありません</p>
<% end %>
