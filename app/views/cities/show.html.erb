<header class="text-center mt-3 container">
  <h1><%= "#{@city.prefecture.name}#{@city.name}の不動産会社" if @city.present? %> </h1>
</header>

<section class="mt-3 container">
  <% if @shops.present? %>
    <%= render @shops %>
  <% else %>
    <p class="alert alert-secondary text-center">このエリアには表示できる不動産会社がありません</p>
  <% end %>
</section>
